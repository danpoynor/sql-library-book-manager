extends ../layout

block content

  form(action="/authors/" + author.id, method="post" autocomplete="off" novalidate)
    include ./form.pug

    a(class="button" href="/authors") Cancel

    a(class="button" href="/authors/" + author.id + "/delete") Delete Author
